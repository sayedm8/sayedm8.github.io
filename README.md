# Kira

Static site for Kira — a web-design studio for Qatar's food scene.
English `index.html` + Arabic `kira-ar.html` + a concept demo in `demos/`.
Regenerate with `deploy/build-deploy.py`; set the live domain with `deploy/set-domain.py`.
